name: test_rando_app
description: Prototype carte rando (patous, chasse, travaux, orage)
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_map: ^7.0.0
  latlong2: ^0.9.1
  geolocator: ^9.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/icons/patou.png
  #   - assets/icons/chasse.png
  #   - assets/icons/travaux.png
  #   - assets/icons/orage.png
